name: Daily Today-in-History Post

on:
  schedule:
    - cron: "00 8 * * *"  # runs every day at 11:00 GMT+3 (Istanbul)
  workflow_dispatch:

# ✅ Restrict token permissions to minimum
permissions:
  contents: write      # only allow commit/push to repo
  actions: read
  checks: read

jobs:
  create-post:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # ✅ Correct path to requirements inside scripts folder
      - name: Install dependencies
        run: pip install -r scripts/requirements.txt

      # ✅ Run post generation script
      - name: Generate daily post
        run: python scripts/generate.py

      - name: Commit and push results
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add scripts/posts/*.md scripts/posts/posts_index.json
          git commit -m "🕰️ Auto-update: Today in History post"
          git push
