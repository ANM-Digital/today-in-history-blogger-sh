name: Daily Today-in-History Post

on:
  schedule:
    # Run every day at 13:15 (1:15 PM) Istanbul time (GMT+3)
    # Note: GitHub Actions uses UTC, so 13:15 GMT+3 = 10:15 UTC
    - cron: "13 10 * * *"
  workflow_dispatch:

jobs:
  create-post:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: 🧰 Install dependencies
        run: |
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        working-directory: ./scripts

      - name: 📝 Generate daily post
        run: python generate_history.py
        working-directory: ./scripts/posts/

      - name: 💾 Commit and push new post
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add .
          git commit -m "🗓️ Auto-generated Today in History post for $(date +'%Y-%m-%d')" || echo "No changes to commit"
          git push
